{
  "compilerOptions": {
    "target": "es5", // 컴파일된 코드가 실행될 JavaScript 버전
    "module": "commonjs", // 모듈 시스템을 정의
    "strict": true, // 엄격한 타입 체킹(아래 주석의 설정들이 자동 활성화)
    // noImplicitAny: 변수의 타입이 암시적으로 any가 되는 것을 방지합니다. 즉, 모든 변수에는 명시적인 타입이 필요하거나 타입 추론이 가능해야 합니다.
    // strictNullChecks: null과 undefined를 모든 타입에서 기본적으로 허용하지 않습니다. 이를 통해 null과 undefined에 대한 체킹이 강화되어 런타임 오류를 줄일 수 있습니다.
    // strictFunctionTypes: 함수 매개변수의 타입이 더 엄격하게 체킹됩니다. 이는 주로 고차 함수에서의 타입 안전성을 강화합니다.
    // strictPropertyInitialization: 클래스의 모든 속성이 생성자에서 초기화되었는지 확인합니다. 이 설정은 strictNullChecks가 활성화되어 있을 때 유효합니다.
    // strictBindCallApply: bind, call, apply 메서드에 대한 엄격한 타입 체크를 활성화합니다.
    // noImplicitThis: this 표현의 암시적인 any 타입을 방지합니다.

    "esModuleInterop": true, // CommonJS와 ES6 모듈 간의 호환성 향상
    "outDir": "./dist" // 컴파일된 파일이 저장될 디렉토리 지정
  },
  "include": ["app/**/*.ts", "app/**/*.tsx"],
  "exclude": ["node_modules"]
}
